{% if prometheus_users is defined and prometheus_users|length > 0 %}
basic_auth_users:
	{% for users in prometheus_users %}
		{{ key }}: {{ value | password_hash('bcrypt') }}
	{% endfor %}
{% endif %}
